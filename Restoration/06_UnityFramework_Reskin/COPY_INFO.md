# Coin Merge Fortune 换皮副本

来源：`../06_UnityFramework`  
创建日期：2026-09-21  
Unity 版本：2022.3.62f3c1

这是用于后续换皮工作的独立 Unity 工程副本。已复制 `Assets`、`Packages`、`ProjectSettings`、`README.md` 及其全部资源文件；为了减小体积并避免携带机器相关缓存，未复制 `Library`、`Logs`、`Temp`、`UserSettings`、`obj`、`Build`、`Builds`。首次用 Unity Hub 打开时会自动重新生成缓存。

副本的 `productName` 已设置为 `CoinMergeFortune_Reskin`，用于与原工程隔离运行时存档。后续换皮请只修改本目录，原工程仍位于 `../06_UnityFramework`。

复制文件数量、字节数和 SHA-256 校验记录见 `COPY_MANIFEST.json`。

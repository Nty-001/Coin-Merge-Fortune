# 主界面 R2 执行范围与交付

用户最新明确请求（2026-09-16）：

> 你先按照这个效果图帮我把主界面换成和这个效果图一致

该请求附带的精确目标图已保存为 `Sources/TargetHome.png`，来自 `codex-clipboard-ba7beba0-dcbb-451d-aea0-92ee5dcdcc50.png`。

筹码标准是用户上一条明确指定的 11 级筹码图 `Sources/Chips.png`。对旧素材库中的筹码源图与本次附件解码后逐字节比对，RGB 像素完全相同，因此使用现有技术切图，不重新生成筹码图案。

本轮判断：最新的直接落地请求授权将这张精确主界面图用于副本主界面修改。执行文档第 2 节也将“用户最新的明确要求”列为第一优先级。本轮不把先前被否决的 AI 全屏草稿当作确认版本。

最终确认（2026-09-16）：用户回复 **“只换美术”**。

范围：只修改 `Restoration/11_UnityReskin` 的主界面美术引用，以及统一筹码图集；目标包含天空、玻璃面板、金边绿按钮、筹码。**保留全部布局、层级、按钮区域、字体设置及玩法；不添加白色落点圆点。**金额、公告、进度、按钮文字继续由本来的原生 Text 与游戏状态生成。截图中的具体金额和硬币堆用于视觉参考，不写死正常游戏存档。

保留：原始 `06_UnityFramework`、弹窗结构、玩法物理参数、合成概率、提现规则、SDK mock、GM、存档逻辑。

共享影响：筹码贴图与筹码骨骼图集会同时改变 Next、主界面 2000 筹码入口、合成规则中的相同筹码图案。其余弹窗换皮不属于本轮范围。

检验：必须 Unity 编译/导入、真实 Play Mode 截图与按钮命中验证成功后才报告完成。所有测试存档使用独立临时 namespace。

## 确认记录

此前自动审批两次阻止含布局和圆点的作者工具，后已提出范围选择。用户选择“只换美术”，故废弃旧工具；当前 `HomeArtOnlyAuthor` 只更新 13 个 Image.sprite 引用，运行时脚本不修改。保存前逐一比较所有非 Image 组件序列化快照，确保 Transform、Text、Button 等完全不变。

纯美术贴图保持原画布尺寸与导入设置；新版圆角轮廓及透明边缘在素材内处理，不改变 RectTransform。新 UI 贴图使用独立 GUID，只绑定主界面；筹码和指定筹码图集保留原 GUID。原始工程不变。

## 已确认并应用的具体素材版本

`HOME_ART_ONLY_R2`，完整候选位于 `PureArt_R2/Assets`，素材清单图为 `PureArt_R2/MATERIALS_R2.png`，文件指纹为 `PureArt_R2/CANDIDATE.json`。此图是独立素材的技术审阅图，不是 Unity 运行截图。

用户随后明确回复 **“确认 R2 素材，应用到副本”**。35 个候选文件已应用，`APPLICATION.json` 记录本次确认，原有 `CANDIDATE.json` 保留提交确认时的文件指纹。逐文件检查实际 Assets 与确认素材 SHA-256 完全一致。

## 验证结果

Unity 2022.3.62f3c1 导入、编译、真实 Play Mode 共 43 项检查通过，包括正常加载入口、11 级筹码加载、动态金额提示与公告、设置/规则/现金提现/筹码提现/Wheel/GM 原生 Button 命中，以及英文和日文显示。

`Verification/` 的 PNG 来自 Unity Camera 实际渲染。`home_us_initial.png` 为正常初始棋盘；其余主界面截图使用冻结筹码构造美术检查场景，金额和摆放均为临时测试数据。覆盖 750×1624 和 941×1672 两种比例。临时存档已清理，未修改正式存档。

`audit_application.py` 检查最终场景/预制体各仅 13 个 Image.sprite 引用改变，原工程 4167 个文件保持一致。运行时代码、已有导入设置、布局、字体、按钮和物理参数均未改变。

交付为 R2 素材应用到原布局；效果图的具体数值和棋盘摆放不属于正常玩家状态。

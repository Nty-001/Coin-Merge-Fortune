# 金库宝箱加载页 — 2026-09-23

按用户提供的金库宝箱效果图制作新的加载界面：保留标题、金库、宝箱、光束和蓝色地面；原效果图中的静态硬币、进度条与文案经内置 imagegen 分层处理。生成结果保存在 `Assets/Resources/VaultLoading/Backdrop.png`，完整提示词见 `imagegen-prompt.txt`。硬币使用已有的 1254 × 1254 RGBA32 高清原图，实际进度条和 Loading... 文案为独立原生 UI。

- 两个 Startup 加载预制体都改成全屏 Overlay Canvas，按最大比例等比覆盖屏幕，移除旧的安全区留边相机和云朵延展。长屏左右有适量背景裁切，标题、硬币、宝箱与进度条保持完整比例。
- 9 枚预制体中的装饰硬币从宝箱口错峰飞出、放大、回正，再轻微浮动。`LoadingCoinBurst` 只更新已有 RectTransform / CanvasGroup，不运行时创建静态 UI、不更改玩法硬币或碰撞。
- `RecoveredStartup` 和 `RecoveredLoadingView` 未修改。进度从 0 到 100% 后才进入游戏；动画不延长加载时间。参考图没有条内百分比，因此加载条只保留黄色进度与下方文字。
- `LoadingReskinAuthor.Run` 为可再次执行的 Editor 制作入口；运行时直接加载已保存的两份真实预制体。

Unity 2022.3.62f3c1 独立校验副本通过正常启动场景进入 Play Mode，分别验证 Rewarded / Packaged 路由。检查资源加载、9 种高清纹理、初始零进度、进度单调增长、完成后跳转、动画随加载场景销毁。941 × 1672、1080 × 1920、1080 × 2340 的实际 GPU 渲染截图已检查，包括逐像素检查顶部/底部未覆盖区域。渲染截图使用测试相机输出纹理，生产预制体保持 Overlay Canvas。未进行 APK 构建或 MuMu 安装。

本地截图、结果 JSON、旧预制体备份和校验副本不进入公开提交。此前整个 12 工程未跟踪，本次检查点提交新素材、制作工具、动画组件和 `loading-prefabs.patch`；补丁已应用于本地预制体，勿重复应用。主界面、提现页与游戏逻辑未修改。
